extends layout

block content
  .container
    .row
      .mid-col-4.top-dd
        h3 Student is
        select(name='')
          option(value='anxious') anxious
          option(value='aggressive') aggressive
          option(value='timid') timid
          option(value='tired') tired
          option(value='hyperactive') hyperactive
      .mid-col-4.top-dd
        h3 Situation is
        select(name='')
          option(value='lecture') lecture
          option(value='group-work') group work
          option(value='independent-work') independent work
          option(value='transition') transition
          option(value='student-presentation') student presentation
  .container.strategy-blocks Strategy list here:
    - const data1 = {name: "affirmation", description: "create sense of belonging", studentState: "timid", situationState: "independent work"}
    h2 does this work
    h3= data1.name
    p= data1.description
    p= data1.studentState
    p= data1.situationState

  #listOfObjects list goes here:

    -  var data = "joshtest3";

    p list is #{data}

    p new field
